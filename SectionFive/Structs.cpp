#include <stdio.h>
#include <iostream>
#include <string.h>
/*using namespace std;
struct Container
{
	string name;
	int x;
	int y;
	int z;
};
struct Character
{
	float health;
	string name;
	int level;
	float damage;
	void Attack()
	{
		cout << name << " level " << level << ", is attacking with " << damage << " dps.\n"<<"His health is: "<<health <<"." << endl;
	}
};
int main(void)
{
	int* myArrayPtr;
	int myArray [] = {1,2,3,4};
	myArrayPtr = myArray;
	cout << "My first position array Ptr content is: " << *myArrayPtr << endl;
	for (size_t i = 0; i < 4; i++)
	{
		cout << "My array Ptr content is: " << *myArrayPtr++ << endl;
	}
	int* myPtr;
	int myInt = 41;
	myPtr = &myInt;
	cout << "My Ptr content is: " << *myPtr << endl;
	cout << "My Ptr address is: " << myPtr << endl;
	Character sebas =
	{
		15.f,
		"Sebas",
		5,
		2.6f
	};
	Character* myCharacterPtr = &sebas;
	cout << "My character name with ptr is: " << (*myCharacterPtr).name<< endl;
	Container myCont = {
		"Sebas",
		4,
		5,
		1
	};
	Container* myContPtr = &myCont;
	cout << "My cont ptr x is: " << (*myContPtr).x << endl;
	cout << "My character health is: " << myCharacterPtr->health << endl;
	return 0;
}*/