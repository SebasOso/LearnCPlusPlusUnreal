#include <stdio.h>
#include<iostream>
using namespace std;
/*enum PlayerStatus
{
	PS_Crouched,
	PS_Running,
	PS_Jumping,
	PS_Walking
};
int main(void)
{
	PlayerStatus status = PS_Running;
	if (status == PS_Running)	
	{
		cout << "The player is running." << endl;
	}
	status = PS_Walking;
	switch (status)
	{
	case PS_Walking:
		cout << "Im walking." << endl;
		break;
	case PS_Running:
		cout << "Im running." << endl;
		break;
	default:
		cout << "Default." << endl;
		break;
	}
}*/ 